name: fem
channels:
    - defaults
    - conda-forge
dependencies:
    - python>=3.10
    - numpy>=1.24
    - scipy>=1.11
    - pandas>=2.1
    - matplotlib>=3.7
    - pytest>=7.0
    - pip

# ── PyTorch (install manually via pip, see https://pytorch.org/) ──────────
# pip3 install torch torchvision torchaudio
#
# ── QUBO solvers (external package) ──────────────────────────────────────
# pip install -e ../qubo-solver
